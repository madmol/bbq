<%= link_to image_tag(photo.photo.thumb.url, class: 'img-thumbnail buffer-bottom'), photo.photo.url,
  data: {
    toggle: 'lightbox', gallery: 'event-photos',
    title: "Добавил #{render partial: 'users/user', object: photo.user} "\
      "#{photo_delete(photo)}"
  } %>
