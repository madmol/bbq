<span class="user">
  <span class="user_avatar">
    <%= image_tag(user_avatar(user), class: 'img-icon') %>
  </span>
  <span class="user-name">
<!--    Пока не понял для чего этот if-->
    <%# if request.path == user_path(user) %>
      <%#= user.name %>
    <%# else %>
      <%= link_to user.name, user_path(user) %>
    <%# end %>
  </span>
</span>
